# Гарантия доступа администратора

**Дата создания:** 2026-01-25  
**Статус:** ✅ Активно

## Описание

Этот документ фиксирует гарантию того, что пользователь с ролью `admin` **ВСЕГДА** получает полный административный доступ ко всем функциям системы, независимо от других условий.

## Текущее состояние (работает правильно)

### Логи подтверждают правильную работу:

```
✅ User profile loaded from /auth/me/:
   - role: admin
   - is_admin: true
   - is_staff: true
   - is_client: false
   - phone: +996700750606
   - counterparty: {type: 'admin', type_display: 'Администратор'}

✅ useUserMenu - returning admin menu with 16 items
```

### Проверки в коде:

1. **Backend (`/auth/me/`):**
   ```python
   'is_admin': user.role == 'admin',
   'is_staff': user.role in ['admin', 'staff'],
   'is_client': user.role in ['tenant', 'landlord', 'investor'],
   ```

2. **Frontend (`useUserMenu.ts`):**
   ```typescript
   // Основная проверка
   const isAdminOrStaff = (user.is_staff || user.is_admin) && 
                          (user.role === 'admin' || user.role === 'staff');
   
   // ДОПОЛНИТЕЛЬНАЯ ГАРАНТИЯ
   if (isAdminRole) {
     // Админ ВСЕГДА получает админское меню
   }
   ```

## Гарантии

### 1. Роль в базе данных
- Пользователь с `role = 'admin'` в таблице `users` всегда имеет административный доступ
- Роль определяется из поля `User.role` в Django модели

### 2. Меню навигации
- Админ **ВСЕГДА** видит полное меню из 16 пунктов:
  - Дашборд
  - Счета
  - Депозиты
  - Недвижимость
  - Контрагенты
  - Договоры
  - Начисления
  - Поступления
  - Отчет
  - Рассылки
  - Заявки
  - Настройки
  - Помощь

### 3. Права доступа
- Админ имеет все разрешения (`can_read_*`, `can_write_*`)
- Админ видит все данные без ограничений (data scoping не применяется)
- Админ может управлять всеми ресурсами системы

### 4. Тип контрагента
- Админ связан с контрагентом типа `'admin'`
- Тип контрагента отображается в боковом меню как "Администратор"

## Защита от ошибок

### Дополнительные проверки:

1. **В `useUserMenu.ts`:**
   ```typescript
   // Если роль 'admin' в БД, ВСЕГДА показываем админское меню
   // Это защита от любых ошибок в данных или логике
   if (isAdminRole) {
     // Принудительно возвращаем админское меню
   }
   ```

2. **В `auth_views.py`:**
   ```python
   # Роль определяется напрямую из БД
   'is_admin': user.role == 'admin',
   ```

3. **В `DataScopingMixin`:**
   ```python
   if user.role == 'admin':
       return queryset  # Админ видит все данные
   ```

## Административный пользователь

**Номер телефона:** `+996700750606`  
**Username:** `admin`  
**Email:** `admin@amt.kg`  
**Роль:** `admin`  
**Контрагент:** "Администратор" (type: `admin`)

## Логирование

Все проверки логируются в консоль браузера:
- `🔍 useUserMenu - user:` - данные пользователя
- `🔍 useUserMenu - isAdminOrStaff check:` - результат проверки
- `✅ useUserMenu - returning admin menu` - подтверждение админского меню
- `🔒 ADMIN GUARANTEE` - принудительное применение админского доступа

## Тестирование

Для проверки правильной работы:

1. Войдите как админ (`+996700750606`)
2. Откройте консоль браузера (F12)
3. Проверьте логи:
   - `role: admin`
   - `is_admin: true`
   - `is_staff: true`
   - `returning admin menu with 16 items`
4. Проверьте боковое меню - должно быть 16 пунктов
5. Проверьте тип контрагента в меню - должно быть "Администратор"

## Изменения

Если в будущем потребуется изменить логику доступа администратора:

1. **НЕ удалять** проверку `isAdminRole` в `useUserMenu.ts`
2. **НЕ изменять** логику определения `is_admin` в `auth_views.py`
3. **НЕ применять** data scoping для пользователей с `role == 'admin'`
4. **Всегда тестировать** вход администратора после изменений

## Статус

✅ **Гарантия активна и работает правильно**  
✅ **Админ всегда получает полный доступ**  
✅ **Логи подтверждают правильную работу**

---

**Последнее обновление:** 2026-01-25  
**Ответственный:** Система гарантирует правильную работу
