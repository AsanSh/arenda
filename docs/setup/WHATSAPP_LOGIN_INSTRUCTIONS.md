# 📱 Инструкция по настройке входа через WhatsApp

## ✅ Что нужно сделать ОДИН РАЗ (для администратора):

### 1. Авторизовать инстанс Green API

1. Войдите в личный кабинет Green API: https://console.green-api.com/
2. Найдите инстанс с ID: `7107486710`
3. В настройках инстанса найдите раздел "QR-код" или "Авторизация"
4. Отсканируйте QR-код в WhatsApp (это авторизует номер для получения сообщений от всех пользователей)
5. После авторизации статус изменится на `authorized`

### 2. Настроить Webhook

1. В настройках инстанса `7107486710` найдите раздел "Webhook"
2. Укажите Webhook URL:
   ```
   http://assetmanagement.team/api/webhooks/greenapi/incoming/
   ```
3. Включите опцию "Входящие уведомления" (Incoming notifications)
4. Сохраните изменения

## 🔄 Как работает вход для контрагентов:

1. **Контрагент открывает** `http://assetmanagement.team/login`
2. **Система генерирует** QR-код с текстом `AMT LOGIN <attemptId>`
3. **Контрагент сканирует** QR-код в WhatsApp
4. **WhatsApp открывается** с предзаполненным сообщением
5. **Контрагент нажимает "Отправить"** → сообщение уходит на номер Green API инстанса
6. **Green API получает** сообщение и отправляет webhook на ваш сервер
7. **Ваш сервер определяет** номер отправителя и находит пользователя в БД
8. **Система автоматически** авторизует контрагента в системе

## ⚠️ Важно:

- ✅ **Авторизуется ОДИН инстанс** (один номер WhatsApp) - это делается один раз администратором
- ✅ **Контрагенты НЕ авторизуются** в Green API - они просто отправляют сообщения
- ✅ **Система автоматически определяет** контрагента по номеру телефона из базы данных
- ✅ **Номер контрагента должен быть указан** в системе (в таблице контрагентов)

## 🔍 Проверка настроек:

### Проверка авторизации инстанса:
```bash
curl "https://api.green-api.com/waInstance7107486710/getStateInstance/6633644896594f7db36235195f23579325e7a9498eab4411bd"
```
Должно вернуть: `{"stateInstance":"authorized"}`

### Проверка webhook:
```bash
curl -X POST "http://assetmanagement.team/api/webhooks/greenapi/incoming/" \
  -H "Content-Type: application/json" \
  -d '{
    "typeWebhook":"incomingMessageReceived",
    "senderData":{"sender":"996557903999@c.us"},
    "messageData":{"textMessageData":{"textMessage":"AMT LOGIN test-123"}}
  }'
```

### Проверка контрагентов в базе:
```bash
cd /root/arenda/infra
docker compose exec backend python manage.py shell -c "from core.models import Tenant; [print(f'{t.name}: {t.phone}') for t in Tenant.objects.filter(phone__isnull=False).exclude(phone='')]"
```

## 🐛 Решение проблем:

### Проблема: "Ожидание подтверждения в WhatsApp..." - не заходит

**Причины:**
1. Инстанс Green API не авторизован
2. Webhook не настроен или не работает
3. Номер контрагента не указан в базе данных
4. Сообщение не было отправлено (пользователь не нажал "Отправить")

**Решение:**
1. Проверьте авторизацию инстанса (см. выше)
2. Проверьте настройки webhook в Green API
3. Убедитесь, что номер контрагента есть в базе данных
4. Попросите пользователя убедиться, что он нажал "Отправить" в WhatsApp

### Проблема: "Номер не зарегистрирован в системе"

**Причина:** Номер телефона контрагента не найден в базе данных

**Решение:** Добавьте номер телефона контрагента в систему через админ-панель
