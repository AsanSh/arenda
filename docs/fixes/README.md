# История исправлений

Этот раздел содержит историю всех исправлений и решений проблем в проекте.

## 📋 Основной файл

**`fixes-log.md`** - единый файл со всеми исправлениями в хронологическом порядке.

## 📁 Архив

Старые файлы `FIX_*.md` и `FIX_*.sh` сохранены здесь для справки, но новые исправления добавляются только в `fixes-log.md`.

## ✍️ Как добавить новое исправление

1. Откройте `fixes-log.md`
2. Добавьте новую запись в начало (после заголовка)
3. Используйте формат:

```markdown
## YYYY-MM-DD - Краткое описание проблемы

**Проблема:** Детальное описание проблемы

**Решение:**
- Пункт 1 решения
- Пункт 2 решения

**Файлы:** `путь/к/файлу1`, `путь/к/файлу2`
```

4. **НЕ создавайте новый файл FIX_*.md!**
